import { icon } from '../../utils'

export const cexIcons = {
  ascendex: icon('AscendEX', () => import('./AscendEX')),
  ftx: icon('Ftx', () => import('./Ftx')),
  mexc: icon('Mexc', () => import('./Mexc')),
}
